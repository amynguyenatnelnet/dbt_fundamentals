sources:
  - name: jaffle_shop
    database: dev_ae_upskilling
    schema: an
    tables:
      - name: raw_customers
      # - name: raw_payments
      - name: raw_orders
        config:
          freshness:
            warn_after: {count: 24, period: hour}
            error_after: {count: 48, period: hour}
          loaded_at_field: _etl_loaded_at